mixin item(id, name, thumbnail, price, isFav)
    li(class="results--item.item")
        h3 #{name} 
        form(action=`toggle-fav/${id}` method="POST")
            button #{isFav ? '💖' : '🤍'}
        img(src=(thumbnail))
        span #{price} €
